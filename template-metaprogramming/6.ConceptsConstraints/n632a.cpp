import std;

namespace n632a {
    void handle(int v)
    { /* do something else */ }

    //void handle(long v)
    //   requires (sizeof(long) > sizeof(int))
    //{ /* do something else */ }
} // namespace n632a

auto main() -> int {
    return 0;
}
